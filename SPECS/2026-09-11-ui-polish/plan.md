# UI polish — plan

1. Update `templates/index.html` and `templates/edit.html` with a shared header, card layout, and status pills. No new routes.
2. Replace `static/style.css` with readable spacing, form grid, and status colors.
3. Re-run add/update/delete checks to confirm behavior is unchanged.
