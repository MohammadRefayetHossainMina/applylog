# UI polish — validation

1. `GET /` and `GET /edit/<id>` include `/static/style.css`.
2. Empty table still shows “No applications yet.”
3. Add, update, and delete still succeed with the same routes and SQLite behavior as the earlier spec folders.
